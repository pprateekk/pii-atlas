# Copy this file to .env and fill in real values. NEVER commit .env.

POSTGRES_USER=atlas
POSTGRES_PASSWORD=change_me
POSTGRES_DB=pii_atlas

# Used by the app / pipelines
DATABASE_URL=postgresql://atlas:change_me@localhost:5432/pii_atlas

# OpenAI (only needed from Phase 2 onward)
OPENAI_API_KEY=sk-put-your-key-here
